from django.db import models

#imports the User model from django.contrib.auth.models.
#Django provides a built-in User model for managing user authentication and authorization.
# You can extend this model or use it as is to handle user-related data.

from django.contrib.auth.models import User